#include<bits/stdc++.h>
using namespace std;
class solution {
    public:
    int adddigits(int num) {
        if(num<=0) return num;
        int count=0;
        for(int i=1;i<num;i++) {
            num[i]= num.split(' ');
            nums[j]= num[i].split(' ');
            sum = num[i]+num[j];
        }
    }
   return num;
}
int main() {
    int num;
    cin>>num;
    int count=0;
    cout<<adddigits(num)<<endl;
    return 0;


} 

